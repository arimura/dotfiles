- 一時ファイルを書き出す場合は作業レポジトリにある `./tmp.local` を利用する。 `/tmp` は利用しない。
- `./tmp.local` は多くの場合に最初から存在しているので`ls`で確認する。
- MEMORY.md に記録しない。
- claude codeの MEMORY は絶対に利用しない。
- 人間の approve が必要になる redirect や expansion が含まれるshellは実行しない

